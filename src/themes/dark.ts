import { theme } from 'antd';
const { darkAlgorithm } = theme;
export default {
    algorithm: darkAlgorithm,
    token: {
        colorBgContainer:'#161d3f',
        colorBgBase: '#161d3f',
        colorBgLayout: '#10152e', // layout gutter
    }
}